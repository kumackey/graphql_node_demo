type Query {
  feed(filter: String): [Link!]!
}

type Mutation {
  post(url: String!, description: String!): Link!
  update(id: ID!, url: String!): Link!
  delete(id: ID!): Link!
}

type Link {
  id: ID!
  description: String!
  url: String!
}